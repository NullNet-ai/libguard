[package]
name = "libdatastore"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.13.4"
tonic = { version = "0.12.3", features = ["tls", "tls-roots"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
base64 = "0.21"

[build-dependencies]
tonic-build = "0.12.3"

